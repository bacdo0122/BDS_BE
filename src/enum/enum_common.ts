export enum role_user{
   user = "user",
   admin = "admin"
}

export enum bds_status{
   pending = "pending",
   finish = "finish"
}
export enum bds_type{
   sell = "sell",
   rent = "rent"
}

export enum status_bds{
   pending = "pending",
   done = "done"
}
